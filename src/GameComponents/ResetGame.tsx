const ResetButton = () => {
    return (  
        <div className="reset">
            <button onClick={() => alert('Reset Game')}>Reset Game</button>  
        </div>
    );
}
 
export default ResetButton;